<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.feidi.video">

    <application>
        <activity android:name=".mvp.ui.activity.InspectWarningActivity">

        </activity>

        <activity android:name=".mvp.ui.activity.TCPTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".app.services.TCPConnectService" />

    </application>

</manifest>
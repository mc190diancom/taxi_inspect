<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/white"
    android:focusable="false"
    android:focusableInTouchMode="false"
    android:paddingStart="13dp"
    android:paddingTop="16dp"
    android:paddingEnd="13dp"
    android:paddingBottom="13dp"
    tools:ignore="all">

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="移除可疑车辆库"
        android:textColor="#333333"
        android:textSize="16sp" />

    <ImageView
        android:id="@+id/iv_close"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentEnd="true"
        android:contentDescription="@null"
        android:scaleType="centerInside"
        android:src="@drawable/ic_close_bottom_dialog" />

    <EditText
        android:id="@+id/et_content"
        android:layout_width="match_parent"
        android:layout_height="79dp"
        android:layout_below="@+id/tv_title"
        android:layout_marginTop="16dp"
        android:background="@drawable/bg_et_remove_reason"
        android:gravity="left|top"
        android:hint="请输入移除原因"
        android:inputType="text"
        android:paddingStart="15dp"
        android:paddingTop="7dp"
        android:paddingEnd="15dp"
        android:textColor="#333333"
        android:textSize="14sp" />

    <Button
        android:id="@+id/btn_confirm_remove"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_below="@+id/et_content"
        android:layout_marginTop="13dp"
        android:background="@drawable/bg_confirm_remove_selector"
        android:enabled="false"
        android:text="确认移除"
        android:textColor="@android:color/white"
        android:textSize="16sp" />

</RelativeLayout>